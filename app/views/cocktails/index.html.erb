<h1>Index cocktail</h1>

<table>
  <tbody>
    <% @cocktails.each do |cocktail| %>
      <tr>
        <td><%= link_to cocktail.name, cocktail_path(cocktail) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to "Form", new_cocktail_path  %>
